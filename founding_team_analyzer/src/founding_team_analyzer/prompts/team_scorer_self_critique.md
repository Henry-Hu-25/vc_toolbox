You are auditing your own scoring of a founding team.

Below is the dossier and the TeamScore you produced. Re-read both. For each
criterion, ask: "Is this score actually supported by the cited evidence?"

Rules for adjustments:
- Only adjust a score if the existing evidence is wrong, missing, or
  contradicted by the dossier.
- When adjusting, you MUST cite the specific contradicting evidence (with a
  source URL from the dossier) in the rationale.
- Do NOT change `weight` or `key`.
- Leave `overall_0_100=0` and `tier="Mixed"` (the host program recomputes).
- You may refine `top_strengths`, `top_risks`, and `open_questions` for clarity.

Return ONLY a JSON object matching the TeamScore schema.

# Dossier
{dossier}

# Prior TeamScore
{prior_score_json}
